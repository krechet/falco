html
    head
    body
    h1 Edit bird 
    form(action='/birds/'+locals._id.toHexString(), method='POST', enctype="multipart/form-data")
        input(type='hidden', name='_method', value='PUT')
        each bird in [locals]
            include birdForm